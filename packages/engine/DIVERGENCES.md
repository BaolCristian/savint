# Divergenze volute rispetto al runtime Numbas

| Area | Upstream | Motore SAVINT | Motivo |
|---|---|---|---|
| Casualità | `Math.random` globale, semina solo via funzione JME `seedrandom` | `Rng` iniettato nello scope, seminato per tentativo con `seedrandom(seed)` | ricalcolo lato server con lo stesso seed |
| Formato d'ingresso | XML compilato o JSON | solo JSON | il compilatore Python resta fuori |
| Messaggi | i18next, catalogo `en-GB` + traduzioni parziali | dizionario `it`/`en` nostro | italiano completo |
| Esame | `exam.js`: navigazione, timer, SCORM, gruppi con pesca | non portato | la composizione la fa SAVINT |
| Primi (`math.js:2214-2215`) | `primes`/`primes_bigints` contengono un baco: `7207` e `7211` concatenati in un unico elemento `72077211` (virgola mancante nel sorgente minificato a mano) → 999 elementi invece di 1000, non ordinato in quel punto | Array corretto: `7207` e `7211` separati, 1000 elementi ordinati (`packages/engine/src/math/number-theory.ts`) | nessun test upstream noto dipende dall'intervallo esatto 7207-7919; la fedeltà al baco avrebbe propagato un errore silenzioso a `factorise`/`divisors`/`largest_square_factor` per ogni numero con un fattore primo in quell'intervallo |
| `setmath` (`math.js:3759-3834`) | `setmath.contains`/`union`/`intersection`/`minus` dipendono da `Numbas.util.eq` (uguaglianza di TOKEN JME con `scope`), che a sua volta dipende da `math.js` (`matrixmath.eq`/`vectormath.eq`/`setmath.eq`) — ciclo `math.js ⇄ util.js` risolto solo dall'ordine di caricamento a runtime | `setmath` opera su valori JS grezzi, con un'uguaglianza iniettabile (`eq?: (a,b)=>boolean`, default `objects_equal` da `predicates.ts`) — nessuna dipendenza da token/scope (`packages/engine/src/math/set.ts`) | il ciclo di dipendenza upstream non è portabile in moduli ESM aciclici; l'uguaglianza di token JME (quella che oggi vive in `util.eq`) si sposta interamente in `jme/scope.ts` (Task 2), coerente con l'inventario §13 |
| `row_echelon_form`/`reduced_row_echelon_form` (`math.js:3614-3723`) | Mutano l'array `matrix` in ingresso in place (swap di righe, riassegnazione celle) | Lavorano su una copia (`matrix.map(r => [...r])` con `rows`/`columns` ricopiati): l'array passato non è mai mutato (`packages/engine/src/math/matrix.ts`) | stile funzionale più sicuro per chi chiama la funzione più volte sulla stessa matrice; verificato che upstream le chiama solo internamente da `gauss_jordan_elimination` (già su una copia via `fraction_matrix`), quindi la mutazione era "locale" e innocua da rimuovere |
| Costruttore `Fraction` (`math.js:2374-2396`) | Se numeratore/denominatore sono `number` non interi, raddoppia entrambi in un loop `while` senza limite, potenzialmente infinito per input come `NaN` | Il loop di raddoppio è limitato a 64 iterazioni, poi lancia `RangeError("Fraction: numeratore o denominatore non convertibile a intero")` (`packages/engine/src/math/fraction.ts`) | evita un blocco del motore su input degeneri; 64 raddoppi coprono ampiamente qualunque frazione binaria rappresentabile in un `number` IEEE 754 |
| `Fraction.common_denominator` (`math.js:2561-2570`) | Chiama `math.lcm(d, f.denominator)` dove `f.denominator` è il getter (un `number`, non `bigNumerator`/`bigDenominator`): dalla prima iterazione del `forEach` in poi `lcm` degrada silenziosamente a calcolare in floating point, perché uno dei due argomenti non è più `bigint` | Il calcolo del denominatore comune resta interamente in `bigint` (`d = lcm(d, BigInt(f.denominator))`, `packages/engine/src/math/fraction.ts`) | evita l'imprecisione silenziosa upstream per denominatori grandi; i numeratori/denominatori finali passano comunque dai getter lossy `f.numerator`/`f.denominator` come upstream, quindi il comportamento osservabile coincide per input ragionevoli — annotato `// upstream:` nel codice |
| `Decimal` (`math.js:23-28`) | `Decimal.set({precision:40, modulo:Decimal.EUCLID, toExpPos:1000, toExpNeg:-1000})` muta la configurazione GLOBALE della classe `Decimal` importata (side effect al caricamento del modulo) | `Decimal.clone({...stessa configurazione...})` crea un costruttore locale in `packages/engine/src/math/complex-decimal.ts`, che tutto `math/` importa; la classe globale di `decimal.js` non è toccata | non si vuole mutare uno stato condiviso con eventuali altri usi di `decimal.js` nell'app ospite (§4 dell'inventario); di conseguenza `new Decimal(...)` dal pacchetto `decimal.js` "nudo" e da `@savint/engine`'s `math` si comportano diversamente (precisione, notazione scientifica) — a differenza di upstream dove sono la stessa classe globale mutata; i test che confrontano precisione a 40 cifre devono usare il `Decimal` esportato da `math`, non quello di `decimal.js` |
| Errori JME (`jme.js`, 42 siti) | `new Numbas.Error(chiave, params?, originalError?)`, definita in `numbas.js:82-95`, che traduce la chiave con `R()` di i18next | `JmeError` (`packages/engine/src/jme/errors.ts`) con la stessa chiave upstream esposta come `err.key`, messaggio prodotto da `t(key, params)` del modulo `i18n/`. La traduzione avviene al lancio, come upstream, quindi usa la lingua PREDEFINITA del processo: `errorMessageIn(e, locale)` ricostruisce il testo da `key`/`params` per chi deve mostrarlo in un'altra lingua, ed è quel che usa tutto `parts/` prima di mettere un errore davanti allo studente | i test upstream confrontano `e.originalMessage`, cioè la chiave non tradotta: `err.key` è l'equivalente stabile. Il testo it/en è nostro (decisione 7 del design doc) |
| Errori di `math.js`/`util.js` (49 siti in `math/`) | `new Numbas.Error(chiave[, params])` come ovunque (`math.js:451`, `util.js:1250`, ...): chiave stabile e messaggio tradotto | `MathError` (`packages/engine/src/errors.ts`), sottoclasse di `EngineError` come `JmeError`, con la stessa chiave upstream in `err.key` e i parametri di `math.random_integer_partition.invalid k`, `math.real interval.invalid string` e `util.formatNumberNotation.unrecognised syntax`. Le 31 chiavi sono in `i18n/it.ts` e `i18n/en.ts` (due — `vectormath.dot.matrix too big` e `vectormath.cross.matrix too big` — non sono letterali al punto di `throw`: `asVector` le riceve come parametro, ed è per questo che erano sfuggite); il testo inglese è quello di `locales/en-GB.json`, tranne `math.toNearest.complex` e `util.permutations.r bigger than n`, che upstream hanno la chiave ma nessun testo in nessun catalogo. La classe base sta sotto `math/` perché `math/` non può importare `jme/` | prima erano `new Error("<chiave>")`: la chiave grezza faceva da messaggio, non stava in nessun catalogo, `engineErrorKeys` non la vedeva (quindi un chiamante non poteva distinguere una domanda malfatta da un guasto del motore) e quel testo arrivava fino al feedback dello studente attraverso `parts/adaptive-marking.ts`. `util.formatNumberNotation.unrecognised syntax` upstream è per giunta un `new Error(chiave, {syntax})` nudo (`util.js:643`), che ignora il secondo argomento |
| Prototipi nativi (`util.js:1684-1726`, 10 usi in `jme.js`) | `Array.prototype.contains`/`merge` e `String.prototype.contains` aggiunti al prototipo globale | `.includes()` per `contains`, e `mergeUnique(a, b, sortfn?)` in `jme/util.ts` con la stessa semantica (unione, ordinamento, dedup) | non si estendono mai i prototipi nativi (decisione 10 del brief): è stato globale condiviso con l'app ospite |
| Dipendenza `jme → jme.display` (`jme.js:422-579`, 3201) | `contentsubvars`/`subvars`/`tokenToDisplayString` chiamano direttamente `jme.display.texify`/`exprToLaTeX`/`treeToJME`/`subvars` e `jme.collectRuleset`, moduli che caricano DOPO `jme-base`: funziona solo perché a runtime tutto è già stato eseguito | `displayHooks` in `jme/subvars.ts`, un oggetto di ganci opzionali che `jme/display.ts` (Task 5) e `jme/rules.ts` (Task 3) riempiono; se il gancio manca il ramo lancia `JmeError("jme.subvars.display not available", {op})` | il grafo dei moduli ESM non ammette una dipendenza in avanti non dichiarata; il fallimento diventa esplicito invece di essere un `TypeError` su `undefined` |
| `typeToDisplayString` (`jme.js:500-529`) | Il dizionario è definito tutto in `jme.js`; i rami `number` e `decimal` costruiscono un `jme.display.JMEifier` risolvendo `Numbas.jme.display` a tempo di chiamata, e il ramo `html` legge `outerHTML` dei nodi del DOM | Il dizionario vive in `jme/subvars.ts` con i soli rami senza dipendenze dal display (`integer`, `rational`, `string`, `html` sulla stringa sorgente); i rami `number` e `decimal` li registra `jme/display-jme.ts` al caricamento, perché `subvars.ts` non può importarlo (ciclo) | stesso comportamento osservabile: chi importa `jme/display.ts` (o l'indice `jme/`) ha il dizionario completo. Senza il modulo di visualizzazione i due rami mancano e `tokenToDisplayString` ripiega sul gancio `treeToJME`, che lancia `jme.subvars.display not available` |
| `Scope.rng` | Nessun campo: le funzioni casuali usano `Math.random` tramite `Numbas.math.randomrange` | Ogni `Scope` ha un `rng: Rng`; la radice senza genitore usa `makeRng("savint")`, un figlio eredita il riferimento del genitore (stessa sequenza), e `ScopeExtras.rng` permette di semizzarlo (`loadQuestion`, Task 9) | valutazioni riproducibili lato server (decisione 5 del design doc); `jme.compare`/`randoms` leggono `scope.rng` invece del generatore globale |
| `Scope.locale` (`localisation.js`, `Numbas.locale.preferred_locale`) | Nessun campo: `R(key, params)` e il builtin JME `translate` traducono con la lingua GLOBALE di i18next, scelta dall'esame | Ogni `Scope` può portare un `locale`: `loadQuestion` lo fissa sullo scope della domanda (`buildQuestionScope`), un figlio eredita quello del genitore, `ScopeExtras.locale` lo sovrascrive e `clone()` lo conserva — esattamente come `rng`. `translate` e i `t()` che hanno uno scope o una parte sottomano leggono quello; `setLocale`/`getLocale` restano solo la predefinita del processo per chi non ne indica nessuna, letta al momento del caricamento. `Question.locale` e `PartBase.locale` sono proiezioni dello scope | la lingua è osservabile a ogni correzione, non solo al caricamento: con una globale, caricare una seconda domanda in un'altra lingua cambiava il feedback della prima, e un `restoreQuestion` senza `locale` riportava tutto il processo in italiano. Lo `Scope` è anche l'unico contesto che il builtin `translate` ha (33 chiamate nei cinque script di correzione incorporati), quindi è l'unico punto di aggancio possibile |
| `jme.function_sets` (`jme.js:2518`) | Il costruttore di `FunctionSet` si registra nel dizionario globale `Numbas.jme.function_sets`, inizializzato in `jme-builtins.js:59` | Nessun registro globale: `FunctionSet` riempie solo sé stessa, e l'insieme finisce negli scope che lo assorbono con `addFunctionSet` (recuperabile con `getFunctionSet`) | nessun consumatore di `jme.function_sets` esiste dentro `jme.js` (solo scritture, §4 e domanda aperta 9 dell'inventario); niente stato globale mutabile |
| `THTML` (`jme.js:3879-3901`) | `value` è un array di nodi del DOM costruiti con `document.createElement`; `isInteractive()` legge `data-interactive` dagli elementi | Tipo opaco: `value` è la sorgente HTML come stringa, `isInteractive()` la ispeziona con una regex, `equalityTests.html` confronta le stringhe | il motore è headless, non c'è `document` (decisione 3 del brief); il rendering resta a chi consuma il token |
| Suddivisione tokenizer/parser (`jme.js:1177-2466`) | Un'unica classe `Parser` con tokenizer e shunting-yard | `tokenizer.ts` contiene solo le tabelle di operatori, le regex e `normaliseName`, ed è una FOGLIA del grafo dei moduli; la classe `Parser` (tokenizer + parser, come upstream) sta in `parser.ts`; lo shunting-yard è in `shunt.ts` come funzioni libere che prendono il parser. Il `tokenise` libero (`jme.tokenise`, jme.js:164-173) è esportato da `parser.ts`, non da `tokenizer.ts` | il grafo di `jme/` è circolare come lo era il namespace `Numbas.jme`, e `standardParser` si costruisce a livello di modulo: le tabelle devono essere raggiungibili senza passare da `parser.ts`, altrimenti entrare dal tokenizer fa valutare `parser.ts` prima che le tabelle esistano. Vincolo anche di dimensione (nessun file oltre le 1000 righe) |
| `TInt → number` (`jme.js:3760-3764`) | `t.originalValue = this.originalValue`: chiamata come `tok.casts[type](tok)`, `this` è la tabella `casts`, quindi il valore è sempre `undefined` (il commento upstream dice che serve a non perdere la stringa originale) | Il valore si prende dal token: `t.originalValue = n.originalValue` | si porta l'intento documentato invece del baco; la catena `integer → number → decimal` non cambia comunque risultato, perché `number_to_decimal` riceve il valore e non il token |
| `jme.compileList` (`jme.js:282-326`) | Ritorna `null` per una stringa vuota, altrimenti `tree[]` | Ritorna sempre `Tree[]`: `[]` per una stringa vuota | firma vincolante del brief (`compileList(expr): Tree[]`); nessun chiamante upstream distingue `null` da lista vuota |
| `jme.makeFast` (`jme.js:5770-5776`) | Nel ramo senza nomi di argomento costruisce la funzione `f` ma dimentica il `return`: `makeFast(tree, scope)` senza `names` produce `undefined` e poi lancia su `Object.defineProperty` | La funzione viene restituita. Inoltre la catena di dispatch srotolata `f1..f5` è sostituita da una singola chiamata con spread | il ramo upstream è morto (nessun chiamante lo usa senza nomi) e crasherebbe; lo spread ha la stessa semantica per 1-5 argomenti. Differenza residua: con zero argomenti upstream inoltra gli argomenti del chiamante a `fn`, qui `fn` è chiamata senza argomenti |
| `jme.compare` (`jme.js:365-368`) | Un ciclo `for(v of Object.keys(scope.allVariables())) { delete vars1[v]; delete vars2[v]; }` che vorrebbe togliere le variabili già definite, ma `vars1` è un array e la cancellazione per nome non fa nulla | Il ciclo non è portato | è un no-op upstream: portarlo come `filter` avrebbe cambiato il comportamento (le variabili definite nello scope smetterebbero di essere campionate) |
| `jme.findvars` senza scope (`jme.js:4836`) | Usa `jme.builtinScope`, inizializzato in `jme-builtins.js:41` | `getBuiltinScope()` in `jme/scope.ts`: uno `Scope` vuoto finché il Task 4 non registra quello dei builtin con `setBuiltinScope` | i builtin arrivano dopo nel porting, e una dipendenza in avanti non dichiarata non è esprimibile in ESM |
| Tabelle di un nuovo `Parser` (`jme.js:1184-1621`) | Sono campi di classe: ogni istanza riparte dai letterali del sorgente, mentre le tabelle globali `jme.precedence` ecc. sono quelle dello `standardParser` | Un nuovo `Parser` copia da `initialTables`, un'istantanea presa al caricamento del modulo; `adoptGlobalTables` fa condividere allo `standardParser` gli oggetti globali, come upstream | stesso comportamento osservabile, ma esplicito: senza l'istantanea un parser creato dopo `standardParser.addBinaryOperator(';')` avrebbe ereditato `;` fra i propri operatori |
| Profondità di ricorsione (`jme.js`, §7.11 dell'inventario) | Nessun limite: uno stack overflow è un `RangeError` nativo | Identico: nessun limite introdotto | decisione 7 del brief; un limite esplicito cambierebbe il tipo di errore visto da chi scrive espressioni |
| Blocco commentato di `simplificationRules` (`jme-rules.js:2207-2227`) | 17 regole per cancellare potenze con la stessa base, dentro un commento `/* ... */`: codice morto, mai eseguito | Non portato (`packages/engine/src/jme/rules-simplify.ts`) | è codice morto (inventario §8.5): portarlo come regole attive cambierebbe il risultato di `all`, portarlo come commento duplicherebbe testo che nessuno può eseguire. Se in futuro serve una semplificazione più aggressiva delle potenze, va riscritto e testato |
| Messaggio di `jme.calculus.unknown derivative` (`jme-calculus.js:172`) | `{tree: jme.display.treeToJME(tree)}`: l'unico punto in cui `jme-calculus.js` dipende da `jme-display.js` | `{tree: <nome della funzione o dell'operatore>}` — o il tipo del token se non ha nome (`packages/engine/src/jme/calculus.ts`) | decisione 6 del brief del Task 3: `calculus.ts` non importa il modulo di visualizzazione (che arriva col Task 5), e il nome della funzione è l'informazione utile per chi scrive la domanda |
| Suddivisione di `jme-rules.js` (2294 righe) | Un unico file | Sei moduli: `rules-terms.ts` (29-455, 1064-1151), `rules-match.ts` (456-1151, 1574-1757), `rules-sequence.ts` (1152-1573), `rules-parser.ts` (1850-1945), `rules-transform.ts` (65-150, 1758-1849), `rules-ruleset.ts` (1946-2108), `rules-simplify.ts` (2109-2294), più il barile `rules.ts` | vincolo del design doc: nessun file oltre le 1000 righe (il solo codice non commentato di `jme-rules.js:1-1757` è già 1139 righe). Il grafo resta circolare come il namespace unico upstream, ma nessun modulo legge simboli di un altro durante l'inizializzazione |
| `matchWhere` (`jme-rules.js:909`) | `util.copyobj(condition, true)`: copia profonda dell'albero **e dei token**, che upstream sono oggetti con proprietà proprie | `copy_tree(condition)` (jme.js:101-107): copia i nodi `{tok, args}` e riusa i token (`packages/engine/src/jme/rules-match.ts`) | nel port i token sono istanze di classe con getter (`TInt.value`) e tabelle di cast sul prototipo: una copia strutturale li spezzerebbe, facendo fallire ogni condizione `` `where `` su un intero. `substituteTree` non muta l'albero, quindi copiare i nodi basta |
| `getTerms` con quantificatore su un sotto-termine associativo (`jme-rules.js:425-429`) | `sub = sub.map(function(t) { t.quantifier = ... })` — il callback non ha `return`: la lista dei sotto-termini diventa una lista di `undefined`, e i termini finiscono comunque nel risultato | Si tiene solo la mutazione del quantificatore, `sub` resta la lista dei termini (`packages/engine/src/jme/rules-terms.ts`) | è un baco upstream che produrrebbe `undefined` dentro l'array dei termini e un crash poco dopo; nessun test upstream lo esercita. Si porta l'intento documentato dal commento ("pull quantifiers through") |
| Rilevamento cicli in `Ruleset.simplify` (`jme-rules.js:2023`) | `notation.treeToJME(exprTree)` per confrontare gli alberi già visti dopo 100 iterazioni | `displayHooks.treeToJME` se il modulo di visualizzazione è caricato (Task 5), altrimenti una firma strutturale locale `tipo:nome(figli...)` (`packages/engine/src/jme/rules-ruleset.ts`) | `Ruleset.simplify` deve funzionare anche senza il modulo di visualizzazione; la firma strutturale distingue gli alberi almeno quanto la loro forma JME, quindi il ciclo viene rilevato allo stesso modo. Cambia solo il testo del parametro `{expr}` del messaggio d'errore |
| `extendOptions` (`jme-rules.js:46-59`) | Scrive sempre tutte le chiavi dell'oggetto opzioni, anche quando il valore è `undefined` | Le chiavi senza valore restano assenti (`packages/engine/src/jme/rules-terms.ts`) | `exactOptionalPropertyTypes` non ammette di assegnare `undefined` a una proprietà opzionale; nessun chiamante usa `in` sulle opzioni, quindi il comportamento osservabile coincide |
| `compileRules` (`jme-rules.js:2265-2273`) | Sostituisce in place le voci dell'array sorgente con gli oggetti `Rule` | Costruisce un array nuovo, lasciando intatta la forma sorgente (`packages/engine/src/jme/rules-simplify.ts`) | la forma sorgente non è più letta da nessuno dopo il bootstrap; l'array literal resta immutabile e leggibile |
| `builtin_function_set` (`jme-builtins.js:60-68`) | Ogni tema costruisce un `jme.FunctionSet`, che finisce sia nello scope (`addFunctionSet`) sia nel registro globale `Numbas.jme.function_sets` (usato solo dall'editor per la documentazione) | `add(scope, name, intype, outcons, fn, options)` in `jme/builtins/registry.ts` chiama direttamente `scope.addFunction`, mentre `functionSet(scope, {name, description}, register)` raccoglie le definizioni del tema in un `FunctionSet` e lo deposita in `scope.function_sets`. Nessun registro globale: gli insiemi vivono solo negli scope. I 27 temi hanno i nomi upstream, anche quando due temi stanno nello stesso modulo (`sets-intervals.ts`, `complex-exponentials.ts`, `number-parsing.ts` registrano un insieme per tema) | il registro GLOBALE non ha consumatori, ma gli insiemi PER SCOPE sì: il builtin `add_function_sets(scope, nomi)` (jme-builtins.js:2599) li cerca con `scope.getFunctionSet(nome)`, quindi senza di essi sarebbe una funzione che non può mai riuscire. L'ordine di registrazione e la risoluzione degli overload non cambiano: `add` registra subito nello scope e `addFunctionSet` non duplica |
| `jme.lazyOps.push` (`jme-builtins.js:958-962`, 2137, 2973) | Push incondizionato: `jme-builtins.js` gira una sola volta al caricamento | `pushLazy(name)` in `jme/builtins/registry.ts` non aggiunge un nome già presente | `registerBuiltins(scope)` deve poter essere rieseguito su uno scope nuovo (i test ne costruiscono di puliti) mentre `lazyOps` resta un array globale condiviso |
| Funzioni casuali (`jme-builtins.js:2928-3006`) | `random`/`deal`/`shuffle`/`shuffle_together`/`weighted_random`/`random_integer_partition` sono `fn` semplici che chiamano `Numbas.math`, il quale usa `Math.random`; `seedrandom(seed, expr)` sostituisce `Math.random` globale, valuta `expr` e ripristina in un `finally` | Tutte usano `options.evaluate` per poter leggere `scope.rng` al momento della chiamata e passarlo alle funzioni di `math/` (che prendono l'`Rng` come ultimo parametro); `seedrandom` valuta il secondo argomento in `new Scope([scope, {rng: makeRng(seed)}])`, senza toccare nulla di globale (`packages/engine/src/jme/builtins/randomisation.ts`) | requisito di determinismo per seme (§8.12 dell'inventario, decisione 5 del design doc): niente `Math.random`, niente monkey-patching. Effetto collaterale voluto: `seedrandom` non consuma più il generatore dello scope chiamante |
| `scientificnumberhtml` (`jme-builtins.js:2057-2070`) | `document.createElement('span')` con `innerHTML` e `data-interactive="false"` | Una stringa: `<span data-interactive="false">…× 10<sup>…</sup></span>` dentro un `THTML` (`packages/engine/src/jme/builtins/number-parsing.ts`) | il motore è headless (§6 dell'inventario); la marcatura è la stessa, e `THTML.isInteractive()` continua a leggere `data-interactive` dalla sorgente |
| `countdp(decimal)` / `countsigfigs(decimal)` (`jme-builtins.js:2162-2164`, 2188-2190) | `a.decimalPlaces()` su una `ComplexDecimal` e `a.re.countSigFigs()` su un `Decimal`: nessuno dei due metodi esiste (sono di `Decimal` il primo, di `Numbas.math` il secondo), quindi le due firme lanciano un `TypeError` | `a.re.decimalPlaces()` e `math.countSigFigs(a.re.toString())` (`packages/engine/src/jme/builtins/number-parsing.ts`) | in TypeScript il codice upstream non compila nemmeno; si porta l'intento (contare cifre decimali/significative della parte reale). Nessun test upstream copre queste due firme |
| `numrows`/`numcolumns` (`jme-builtins.js:609/612` e `632/635`) | Registrate due volte con la stessa firma `[matrix] → number`: la seconda coppia (quella che chiama `matrixmath.numrows`/`numcolumns`) è irraggiungibile, perché a parità di punteggio vince la definizione registrata per prima | Registrate una volta sola, con un commento `// upstream:` al posto della seconda coppia (`packages/engine/src/jme/builtins/linear-algebra.ts`) | codice morto (§8.9 dell'inventario); le due implementazioni sono identiche (`m.rows` / `m.columns`) |
| `matrixmath.fraction_matrix` (`math.js:3582-3591`) | `new Fraction(c, 1)`: con entrambi gli argomenti `number` il costruttore raddoppia numeratore e denominatore finché non sono interi | Il port del Task 1 passava `1n` (un `bigint`), saltando quel ciclo: ogni cella non intera diventava `0` e `inverse`/`gauss_jordan_elimination` davano risultati sbagliati sulle matrici con componenti decimali. Corretto in `new Fraction(c as number, 1)` | baco del port, non una divergenza voluta: trovato dal test `inverse di una matrice con componenti non intere` (jme-tests.mjs:1305) del Task 4a |
| Temi `http` (`jme-builtins.js:3785-3812`) e `promises` (3815-3824) | `fetch_text`/`fetch_json` fanno I/O di rete con la `fetch()` globale; `then` incatena una lambda JME su una `TPromise` prodotta da quelle | Non portati: `registerBuiltins` non li registra | il motore deve girare anche lato server, in modo deterministico e senza rete durante la correzione (§6 dell'inventario). Senza `http` il tema `promises` non ha più consumatori. Se in futuro serviranno, il client HTTP va iniettato esplicitamente invece di usare la `fetch()` globale |
| Tema `html` (`jme-builtins.js:2769-2924`) | 7 nomi: `html`, `isnonemptyhtml`, `image`, `escape_html`, `table` (3 overload), `max_width`, `max_height` | Solo `isnonemptyhtml` ed `escape_html`, in `builtins/html-pure.ts`. `html`, `image`, `table`, `max_width` e `max_height` non sono portati (elenco anche nel commento in testa al modulo) | costruiscono nodi del DOM (`document.createElement`) e, per `html`/`image`, passano da `jme.variables.DOMcontentsubber`: il motore è headless (§6 dell'inventario). Il rendering resta a chi consuma i token. `escape_html` upstream fa l'escaping mettendo un nodo di testo in un `<p>` (protegge solo `& < >`); il port usa `util.escapeHTML`, che protegge anche `"` e `'` |
| `jme.notations` (`jme-notations.js`, usato da `jme-builtins.js:78-84`) | `get_notation(nome)` legge il registro `Numbas.jme.notations`, che `jme-notations.js` riempie con 6 notazioni alternative (set_theory, square_brackets_grouping, ...) | `get_notation` in `jme/builtins/registry.ts` conosce solo `standard` (il parser predefinito e il gancio `displayHooks.treeToJME`); qualunque altro nome lancia `jme.func.parse.no notation` | `jme-notations.js` dipende da `jme-display.js` e non è in questo batch: `parse(str, notazione)` e `string(expr, flags, notazione)` restano quindi limitati a `standard` finché non viene portato |
| `debug_log` (`jme-builtins.js:2538-2543`) | `console.log('DEBUG ' + args[1].value + ':', unwrapValue(args[0]))` e ritorna il primo argomento | Ritorna il primo argomento senza stampare (`builtins/jme-introspection-2.ts`) | il motore gira anche lato server e non deve scrivere sulla console del processo ospite; il valore di ritorno — l'unica cosa osservabile in JME — è identico |
| `translate` (`jme-builtins.js:1805-1810`) | `R(s)` / `R(s, params)`, la funzione globale di i18next caricata da `localisation.js` | `t(s)` / `t(s, params)` del modulo `i18n/` (`builtins/strings.ts`), con la stessa firma JME | decisione 7 del design doc (§8.15 dell'inventario): i18next è sostituito dal dizionario `it`/`en` del motore. Una chiave assente ritorna la chiave stessa, come già fa `t()` |
| `expression(str)` (`jme-builtins.js:2331-2372`) | Salva `Numbas.locale.default_number_notation`, la forza a `['plain']` per la durata della valutazione e la ripristina in un `finally` | Nessun salvataggio/ripristino: la notazione numerica predefinita del port è già `"plain"` (`math/format.ts`, decisione 4 del brief del Task 1) | non c'è una variabile globale di locale da proteggere; il comportamento osservabile coincide |
| `make_variables` (`jme-builtins.js:2500-2521`) | Registrata dal tema `jme` insieme alle altre 39 firme, dentro `builtin_function_set({name:'jme',...}, ...)` | Non registrata da `builtins/jme-introspection.ts`: la registra `variables/builtins.ts` (Task 6) con `add(...)` diretto (non dentro `functionSet(...)`, perché l'insieme `"jme"` è già chiuso da `jme-introspection.ts`), poi la aggiunge a mano all'array `.functions` del `FunctionSet` "jme" già costruito | dipende da `jme.variables.makeVariables`, che era nel batch successivo al Task 4b; il tema `jme` resta altrimenti completo. `scope.getFunctionSet("jme")` include comunque `make_variables` (il push manuale lo garantisce): `add_function_sets(s, ["jme"])`, un builtin raggiungibile da JME, funziona correttamente |
| `make_lambda` (`jme-builtins.js:3275-3712`) | Le funzioni che riscrivono `map(espr, nome, lista)` nella forma con lambda vivono in `options.make_lambda` del `funcObj`, e `findvarsOps` le richiama con `fn.options.make_lambda(...)` | Sono funzioni di modulo in `builtins/comprehensions.ts`, chiamate direttamente sia da `evaluate` sia da `findvarsOps` | `FuncObjOptions` resta la superficie tipizzata del Task 2 senza una chiave in più; il comportamento è identico perché upstream non sostituisce mai `options.make_lambda` dall'esterno |
| `substituteTreeOps.iterate_until` (`jme-builtins.js:3625`) | `jme.substituteTree(tree.args[i+3], scope.allowUnbound)`: passa `scope.allowUnbound` (che non esiste) COME SCOPE, quindi la chiamata lancerebbe appena `iterate_until` riceve il quinto argomento | `substituteTree(args[i+3], scope, allowUnbound)`, come negli altri gestori (`builtins/comprehensions.ts`) | è un refuso upstream: il ramo è raggiungibile solo con `iterate_until(..., max)` dentro un'espressione da sostituire, e lì crasherebbe. Si porta l'intento |
| `diff` (`jme-builtins.js:3754`) | Firma dichiarata `[TExpression, String]`, con la `String` GLOBALE di JavaScript al posto di `TString`: `parse_signature` la riduce a `sig.type(undefined)`, che non combacia con nessun argomento | `[TExpression, TString]` (`builtins/differentiation.ts`) | `diff` è pigra e le funzioni pigre non passano dal controllo di tipo, quindi upstream il refuso è innocuo; scriverlo com'è in TypeScript non compilerebbe |
| `frequencies` (`jme-builtins.js:1531`) | `util.eq(item[0], x)` senza il terzo argomento: per i tipi la cui uguaglianza richiede lo scope (`name`, `expression`, `set`, `dict`, `list`) `util.eq` lancerebbe | `eq(item[0], x, scope)` con lo scope della chiamata (`builtins/lists.ts`) | in TypeScript `eq` richiede lo scope; passarlo rende `frequencies` utilizzabile su liste di quei tipi invece di farla crashare |
| `values(dict, chiavi)` (`jme-builtins.js:1618`) | `new Numbas.Error('jme.func.listval.key not in dict', {key: key})` dove `key` è il TOKEN, non la stringa: il messaggio contiene `[object Object]` | `{key: key.value}` (`builtins/dictionaries.ts`) | il segnaposto `{key}` del messaggio vuole il nome della chiave; il resto (la chiave di errore, su cui si basano i test) è identico |
| `substituteTreeOps.take` (`jme-builtins.js:3709-3715`) | Copia una SECONDA volta `tree.args` in una variabile locale, sostituisce lì dentro e ritorna `{tok, args}`; ma `jme.substituteTree` (`jme.js:247-251`) scarta il valore di ritorno del gestore e usa l'albero che ha copiato lui, quindi la sostituzione dentro `take` non ha alcun effetto | Il gestore muta la copia che il chiamante gli passa (`builtins/comprehensions.ts`), come gli altri otto gestori di `substituteTreeOps` | il ramo upstream è un no-op silenzioso: `take(n, cond, x, lista)` dentro un'espressione da sostituire non vedrebbe mai i valori delle variabili di `lista`, a differenza di `map` e `for:` che nella stessa posizione funzionano. Si porta l'intento del codice (le due `substituteTree` che upstream scrive e poi butta via), non il baco; coperto dall'assert `take si può valutare due volte con liste diverse` di builtins-control-flow.test.ts |
| `jme.subvars`/`contentsubvars` con sostituzione nulla (`jme.js:420`, 570) | `scope.evaluate(expr)` di un'espressione vuota ritorna `undefined`; upstream lo passa a `jme.display.texify({tok: undefined})` o a `treeToJME`, che producono stringhe come `undefined` o lanciano un `TypeError` dentro il renderer | `Scope.evaluate` dichiara `Token \| null`: `subvars` e `contentsubvars` (`jme/subvars.ts`) intercettano il caso e lanciano `JmeError("jme.subvars.null substitution", {str})` | conseguenza della firma onesta scelta nel Task 2 (`evaluate` può ritornare `null`); il fallimento diventa esplicito e localizzato invece di propagare un `undefined` fino al display. Coperto da `jme-subvars-tex.test.ts` |
| Suddivisione di `jme-display.js` (2479 righe) | Un unico file | Quattro moduli: `display-tex.ts` (151-858: `number_options`, `string_options`, gli helper TeX, `texOps`, `texNameAnnotations`, `specialNames`, `typeToTeX`, `flatten`), `display-texifier.ts` (860-1046 `JMEDisplayer` → `Displayer<TOut>`, 1048-1648 `Texifier`/`texify`), `display-jme.ts` (1650-2333), `display.ts` (26-149, l'API di alto livello, più la registrazione dei ganci) | vincolo del design doc (nessun file oltre le 1000 righe) e risoluzione 6 del brief. La classe base sta con il `Texifier` perché così il grafo degli import resta aciclico (`display-tex` → `display-texifier` → `display-jme` → `display`) e chi importa `display.ts` ottiene il modulo completo, ganci compresi |
| Soglia della notazione scientifica (`jme-display.js:1189`, 1251, 1313, 1356, 2177, 2246) | Il letterale `20` ripetuto identico in sei punti | Una sola costante `NICE_NUMBER_MAX_LENGTH = 20`, esportata da `display.ts` | è lo stesso valore magico in tutti e sei i punti (inventario §9): una costante lo rende ispezionabile e impedisce che i sei si separino |
| `jme.display.registerType` (`jme-display.js:1891-1911`) | Punto di estensione per registrare i renderer `tex`/`jme`/`displayString` di un tipo di token custom | Non portato: `typeToTeX` e `typeToJME` sono dizionari statici e completi per i tipi del motore | i tipi di token sono fissi e noti a compile-time (decisione 5 del brief); un registro dinamico sarebbe stato globale mutabile senza consumatori |
| `align_text_blocks`/`tree_diagram` (`jme-display.js:2336-2471`) e alias `jme.display.{Rule,getTerms,matchTree,matchExpression,simplificationRules,compileRules}` (2473-2479) | Utility ASCII-art per stampare un albero in console, e sei alias di retrocompatibilità verso `jme.rules.*` | Non portati | i primi sono strumenti di debug per la console, non display matematico; i secondi sono puntatori, e i nomi vivono già in `rules.ts` (inventario §7) |
| `jme-notations.js` nel display (`jme-display.js:84-148`) | `simplifyExpression`/`simplify`/`subvars` prendono una `Notation` e le delegano `compile`/`treeToJME`/`subvars` | Le stesse funzioni prendono al più un `Parser` opzionale (`display.ts`); `subvars(expr, scope)` è il corpo di `Notation.prototype.subvars` (jme-notations.js:71-113) per la notazione standard | il JSON di domanda non seleziona notazioni e i tipi di parte in ambito usano il parser standard (decisione 1 del brief). Vedi anche la riga su `get_notation` |
| `Numbas.locale.default_list_separator` (10 usi in `jme-display.js`) e `default_number_notation` | Globali impostate da `localisation.js`, che cambiano il separatore delle liste e lo stile numerico di tutto il display | Nessuna globale di FORMATO: il separatore è la costante `LIST_SEPARATOR = ","` (`display-tex.ts`) e la notazione numerica predefinita è `"plain"` (`math/format.ts`), in tutte le lingue. La lingua dei MESSAGGI invece esiste, ma non è una globale: viaggia sullo scope (`Scope.locale`, riga qui sopra) | la localizzazione del player è del Task 9 (decisione 4 del brief del Task 1). I test upstream che forzano la locale (`Localise number representation`, `localisation doesn't affect treeToJME`) sono tradotti con i valori della locale predefinita |
| Ramo `negated()` di `texConstant`/`JMEifier.constant` (`jme-display.js:1543-1546`, 2046-2049) | `util.eq(negated(tok), c.value, scope)`: `negated()` ritorna un numero grezzo, senza `.type`, quindi `findCompatibleType(undefined, ...)` fallisce e il confronto è sempre falso — il ramo è morto (verificato sul runtime upstream: `texify(-e)` dà `-2.7182818285`, non `-e`) | Il ramo resta, ma la condizione è la funzione `eqMaybeUntyped()`, che documenta il fatto e ritorna sempre `false` | in TypeScript non si può passare a `eq(a: Token, ...)` un numero grezzo; riprodurre il ramo "attivo" avrebbe cambiato il comportamento (`-e` sarebbe stato reso come `-e`) |
| `JMEDisplayer.complex_decimal`/`rational_decimal`/`real_decimal` (`jme-display.js:1002-1029`) | Metodi vuoti sulla classe base, che il `JMEifier` non sovrascrive perché sovrascrive `decimal()` per intero | `Displayer` li dichiara astratti; il `JMEifier` li implementa con tre tappi che lanciano se qualcuno li raggiungesse | una classe base con metodi che ritornano `undefined` non è tipizzabile con `TOut`; i tre tappi sono irraggiungibili e lo dicono |
| `collectRuleset` con le sole `flags` (`jme-rules.js:2067-2069`) | `if(set.rules) { set = set.rules; }`: con un oggetto `{flags:{...}}` senza `rules`, `set.length` è `undefined` e il ciclo non parte | Il port del Task 3 scriveva `items = set.rules` senza guardia, e `items.length` lanciava. Corretto in `items = set.rules ?? []`, e `RulesetSpec` ammette esplicitamente `{flags?, rules?}` | baco del port, non una divergenza voluta: trovato dai test `Display > tree to JME` (jme-tests.mjs:2484-2485), che passano `{flags:{noscientificnumbers:false}}` come ruleset |
| `TDecimal.precisionType`/`precision` (`jme.js:3793-3798`) | Proprietà scritte dinamicamente sui token (`texOps.decimal`, `precround`, `dec("21.0")`) | Dichiarate su `TDecimal` come su `TNum` (`jme/tokens.ts`) | erano già scritte a runtime dai builtin attraverso cast `WithPrecision`; `number_options` (display) le legge, e senza la dichiarazione TypeScript rifiutava l'accesso |
| `variable.error in variable definition` (`question.js:831-835`) | `try { tree = jme.compile(definition) } catch { q.error('variable.error in variable definition', {name}) }`: il `catch` non riceve l'errore e non lo passa a `q.error`, quindi la causa vera (un errore di sintassi JME con la sua chiave) è persa — resta solo il nome della variabile | L'errore originale è il terzo argomento di `error(...)` (`packages/engine/src/question/variables.ts`), quindi `engineErrorKeys` lo espone dopo `variable.error in variable definition` | il messaggio esterno da solo non dice all'autore della domanda COSA c'è di sbagliato nella definizione; la catena delle chiavi è il meccanismo con cui il port sostituisce `originalMessages`, e qui costa un argomento |
| Generazione variabili a promesse (`jme-variables.js:256-321` `computeVariablePromise`, 410-469 `makeVariablesPromise`) | Versione `async` di `computeVariable`/`makeVariables`, usata per funzioni JME asincrone (`TPromise`, cache `todo[symbol_promises]`) | Non portata: `variables/generate.ts` ha solo le versioni sincrone | decisione 1 del brief del Task 6: nessun tipo di parte in ambito è asincrono (inventario 05 §6.9); il test upstream `Promises > makeVariablesPromise` (jme-tests.mjs:2833) resta fuori |
| `DOMcontentsubvars`/`DOMcontentsubber` e la coda di `DOMsubvars` (`jme-variables.js:697-700`, 775-782, 941-1207) | Sostituzione di variabili dentro un `Element` del DOM vero: cammina i nodi, clona/marca i nodi HTML inseriti, gestisce `<img>`→`<object>` per SVG, `data-jme-visible`, attributi `eval-*` | Non portati: `variables/subvars.ts` ha solo `substituteHtml(html, scope)`, che lavora su stringhe (nessun attributo, nessuna gestione di `<script>`/`<style>`/`<iframe>`, nessun `data-jme-visible`) | decisione 4 del brief del Task 6: il pacchetto engine non tocca `document`/`window`. `substituteHtml` copre la sostituzione di `{espr}` (con la stessa serializzazione per tipo di `doToken`) e di `\var{}`/`\simplify{}` dentro i blocchi matematici; il rendering/l'inserimento nel DOM resta al player (sotto-progetto 3) |
| `makeJavascriptFunction` (`jme-variables.js:87-134`) | `new Function(paramNames, fn.definition)` sempre permesso, nessun controllo sul linguaggio della funzione | Gated da `options.allowJavascript` (predefinito `true`): con `false`, `makeFunction` lancia `jme.variables.javascript function not allowed` prima di costruire la funzione. Le funzioni marcate `type:"promise"`, o che restituiscono un `Promise`/valore `TPromise` a runtime, lanciano `jme.variables.async function not supported` (nessun equivalente upstream: qui serve perché `makeVariables` è solo sincrono) | decisione 5 del brief del Task 6: molte domande pubbliche usano funzioni JavaScript personalizzate e il contenuto è dei docenti (fiducia esplicita), ma il Task 9 deve poter disabilitarle per contenuti non fidati (`LoadOptions.allowJavascriptFunctions`) |
| `makeFunctions` (`jme-variables.js:172-182`) | Il JSDoc dichiara `@returns {Object<funcObj>}` (un `funcObj` per nome), ma il valore reale è `scope.functions`, cioè un array di definizioni per nome — confermato da `question.js:801-802`, che lo passa direttamente come `{functions: ...}` a `new Scope(...)` | Il port ritorna `Record<string, FuncObj[]>` (non `Record<string, FuncObj>` come nello schizzo del brief), coerente con `scope.functions` e con `ScopeExtras.functions` | il brief riprendeva il JSDoc upstream, impreciso; un dizionario a singolo valore avrebbe perso gli overload con lo stesso nome e non sarebbe stato utilizzabile direttamente da `new Scope([scope, {functions: makeFunctions(...)}])` come farà il Task 9 |
| `Script.prototype.evaluate_note` (`jme-variables.js:920-926`) | Ritorna `{value, scope}` direttamente da `remakeVariables`, SENZA passare per `process_result` (a differenza di `evaluate`, che lo fa) | `NoteScript<TResult>.evaluate_note` ha la firma `(note, scope, variables?) => {value: Token \| undefined; scope: Scope}`, non `TResult` come nello schizzo del brief | lo schizzo del brief usava `TResult` per entrambi i metodi, ma il codice upstream che il Task 7 deve poter riusare tal quale non fa così: `evaluate_note` upstream non chiama mai `process_result`. Si è preferita la fedeltà al comportamento osservabile su cui il Task 7 farà affidamento |
| Chiavi d'errore delle funzioni JavaScript personalizzate (`jme-variables.js:113-133`) | Il `catch` di `makeJavascriptFunction` avvolge OGNI eccezione del corpo (inclusa la propria `jme.user javascript.returned undefined`, lanciata nello stesso `try`) in `jme.user javascript.error`: upstream, `returned undefined` non è mai osservabile come chiave a sé — arriva sempre come `message` interpolato dentro `jme.user javascript.error` | Stesso comportamento: il `catch` avvolge tutto in `jme.user javascript.error`, con UNA sola eccezione — `jme.variables.async function not supported` (non upstream, decisione 3) passa non avvolta, riconosciuta per chiave (non con `instanceof JmeError`, che avrebbe incluso anche `returned undefined` e ogni altro `JmeError` sollevato dal corpo utente) | è la nostra unica divergenza deliberata in quel percorso: nasconderne la chiave dentro `jme.user javascript.error` toglierebbe al chiamante il modo di distinguere "la funzione è asincrona" da un errore generico |
| `makeConstants` per i builtin (`jme-builtins.js:41-57`) | Un'unica `jme.variables.makeConstants` | Due copie: `variables/constants.ts` (Task 6) esporta quella completa (algebra su `def.enabled` ed `enabled`); `jme/builtins/constants.ts` ne tiene una privata, un sottoinsieme (nessun `enabled` per-domanda, i builtin non ne hanno uno) | `jme/builtins/` è letto da `jme/index.ts` prima che `variables/` esista: importare `variables/` da `jme/builtins/` chiuderebbe un ciclo `jme → variables → jme`, la stessa classe di problema (TDZ) già incontrata nella suddivisione di `display.ts` e di tokenizer/parser. La duplicazione (~15 righe) è deliberata; le due vanno tenute in sincronia se l'algebra su `def.enabled` cambia |
| `computeRuleset` su un nome di display flag (`jme-variables.js:539-541`) | Ritorna `undefined`: il chiamante (il `forEach` sugli elementi di un'altra lista di ruleset) ignora il valore di ritorno — serve solo a evitare `ruleset.set not defined` quando un elemento della lista è una flag di visualizzazione (es. `"!fractionnumbers"`), non un ruleset annidato | Ritorna un `Ruleset` vuoto (`new Ruleset([], {})`): la firma di `computeRuleset` nel brief non è nullable | mai osservabile dall'esterno: quel ramo è raggiunto solo come guardia di ciclo per un elemento di lista, il cui valore di ritorno non è mai usato né da `computeRuleset` né da `makeRulesets` (un nome di flag non è mai una vera chiave di `todo` in quel punto) |
| `ScriptNote.description` (`jme-variables.js:813`) | `undefined` quando la nota non ha la parte fra parentesi (`nome: espr`, senza descrizione) | `""` (stringa vuota) | rispetta `description: string` del brief (non `string \| undefined`); nessun consumatore upstream (`marking.js`, fuori scope) distingue "assente" da "vuota" |
| `Script.prototype.evaluate`/`construct_scope` con `variables` assente (`jme-variables.js:900-909`, 911-918) | `Object.keys(this.notes).forEach(name => { if(variables[name] === undefined) ... })`: se `evaluate(scope)` è chiamato senza secondo argomento, `variables` è `undefined` e `variables[name]` lancia un `TypeError` | `variables ?? {}`: `evaluate(scope, variables?)` funziona anche senza `variables` | la firma del brief rende `variables` opzionale anche in `evaluate` (il suo stesso test lo chiama così); senza il fallback il port erediterebbe un crash upstream su un uso che il brief dichiara valido |
| `nscope` morto in `makeJMEFunction` (`jme-variables.js:53-54`) | `const nscope = new jme.Scope([scope]); nscope.addFunction(fn);`: costruisce uno scope figlio e ci registra la funzione in via di definizione | Non portato | `nscope` non è più letto da nessuna parte upstream: né dalla chiusura restituita (che valuta nello scope della CHIAMATA, non in `nscope`), né da `findvarsOps`. Verificato che la ricorsione funziona comunque (la funzione si risolve tramite lo scope in cui è stata effettivamente registrata da `makeFunctions`/dal chiamante, non tramite `nscope`) — copertura: il test del `findvars` ricorsivo in `variables.test.ts` |
| Script di correzione `.jme` (`marking_scripts/*.jme`) | Caricati a runtime come risorse (`Numbas.raw_marking_scripts`, riempito dal compilatore Python che li inietta nel bundle) | Copiati verbatim in `packages/engine/src/marking/scripts/*.jme` (con un'intestazione di licenza in commento `//`, che il parser delle note toglie) e incorporati in `marking/scripts/index.ts` dal generatore `scripts/engine/embed-marking-scripts.mjs`. Nessun `?raw`, nessuna configurazione di bundler, nessuna lettura da filesystem a runtime | il motore deve funzionare identico in Node, nel browser e dentro un bundle, senza dipendere dal compilatore Python né da un plugin di build. Un test (`marking-scripts-embedded.test.ts`) rigenera l'output in memoria e lo confronta col file su disco, così una modifica a uno script senza rilanciare il generatore fa fallire la suite. `matrixentry.jme` resta fuori (tipo di parte `matrix` non portato) |
| `check_pre_submit` (`marking.js:348-366`), `pre_submit`, `TPromise` | Unico punto in cui la correzione può diventare asincrona: `part.do_pre_submit_tasks` può ritornare una promessa, avvolta in un `TPromise` | Non implementati. La funzione JME `check_pre_submit` non esiste: la nota `pre_submit` di `gapfill.jme` (l'unica in ambito che la usa) fallisce e resta registrata come nota non valida in `stateErrors`, senza toccare la nota `mark` | inventario 05 §6.9: nessuno dei tipi di parte in ambito attiva davvero il percorso asincrono (`pre_submit` di gapfill chiama `check_pre_submit` sui gap, che ritorna una promessa solo se il gap stesso ha una nota `pre_submit` — e nessun tipo in ambito ce l'ha). Tenere l'API di correzione sincrona (decisione 1 del Task 6) vale più della fedeltà a un ramo morto |
| `apply_marking_script` (`marking.js:368-404`) | Esegue ricorsivamente un altro `MarkingScript` per nome, cercandolo in `Numbas.raw_marking_scripts` | Non implementata | inventario 05 §11.6: non è usata da nessuno dei sei script in ambito né da nessun test upstream; esiste per il marking personalizzato via plugin. Senza un registro globale di script per nome non avrebbe nemmeno dove cercare |
| Registrazione delle funzioni di stato (`marking.js:471-481`) | Il costruttore di `StatefulScope` registra da sé le 24 `funcObj` del linguaggio delle note | `StatefulScope` è solo lo scope con lo stato; le funzioni le registra `makeMarkingScope(parent, variables?)` in `marking/note-functions.ts` | `note-functions.ts` ha bisogno di `StatefulScope` (per il tipo dello scope da risalire) e la dipendenza inversa chiuderebbe un ciclo fra i due moduli ESM. `makeMarkingScope` è l'unico percorso con cui il motore costruisce uno `StatefulScope` (lo usa `construct_scope` di `MarkingScript`), quindi il comportamento osservabile non cambia |
| Funzione di stato fuori da uno script di correzione (`marking.js:122-125`, 289-292, 336) | `while(p.state === undefined) p = p.parent;` va in `TypeError` in cima alla catena; `scope.question.getPart(path)` idem se non c'è una domanda | `findStatefulScope`/`getPart` lanciano un `JmeError` con chiave (`marking.state function outside marking script`, `marking.no question in scope`, entrambe nostre) | un `TypeError` non tradotto arriverebbe fino allo studente come "Cannot read properties of undefined"; sono i due soli punti del modulo in cui il port aggiunge una chiave non upstream |
| `finalise_state` con credito assente (`marking.js:630`, 638, 642) | `Fraction.fromFloat(state.credit)` senza controlli: un item `set_credit`/`add_credit`/`sub_credit` senza `credit` produrrebbe `NaN` | `creditFraction(undefined)` vale `Fraction.zero`; un `credit` già `Fraction` è usato tale e quale (`FeedbackItem.credit` ammette `number \| Fraction`) | ramo puramente difensivo: nessun costruttore di `feedback` produce un item di credito senza `credit`. Il tipo del brief ammette `Fraction`, e `Fraction.fromFloat` su una `Fraction` sarebbe un errore silenzioso |
| `finalise_state`, riassegnazione del parametro (`marking.js:658`) | Espandendo un `concat` riassegna il parametro `states` con la lista espansa | La lista di lavoro è una variabile locale (`let list = states`) | il chiamante non deve vedere la propria lista sostituita; nessuna differenza osservabile (upstream la riassegnazione è comunque locale alla funzione) |
| `mark_part` in attesa di pre-submit (`marking.js:421-431`) | Se `part_result.waiting_for_pre_submit`, ritorna un risultato vuoto con `valid: false` | Ramo non portato: `mark_part` chiama sempre `finaliseState(part_result.states["mark"] ?? [])` | conseguenza diretta della riga su `check_pre_submit`: senza il meccanismo asincrono nessuna correzione può restare in attesa. Il `?? []` è del port: upstream `finalise_state(undefined)` andrebbe in `TypeError`, qui una nota `mark` mancante dà uno stato vuoto. È difensivo — `setMarkingScript` rifiuta già uno script senza la nota `mark` (`part.marking.missing required note`) — ma se quel controllo saltasse, upstream si accorgerebbe del guasto e il port no |
| `marking_script_result` (`marking.js:501-509`, `process_result` 588-595) | `{states, values, state_valid, state_errors}` | `MarkingScriptResult` ha anche `scope: StatefulScope` | è l'unico modo per raggiungere lo stato completo della valutazione (il Task 8 ne ha bisogno per costruire il feedback della parte), e `evaluate_note` upstream ritorna già lo scope. Additivo: nessun campo upstream è tolto o rinominato |
| `MarkingScript.evaluate_note` | Eredita la firma di `Script.prototype.evaluate_note`: ritorna `{value, scope}` con `scope` di tipo `jme.Scope` | Ristretta a `{value: Token \| undefined; scope: StatefulScope}` | `construct_scope` di uno script di correzione produce sempre uno `StatefulScope`, ed è da lì che il Task 8 legge `stateErrors.pre_submit` (`part.js:1917-1921`). Nessun cambio di comportamento, solo un tipo più preciso |
| `FeedbackItem.reason` | Il typedef (`marking.js:28`) parla genericamente di "correct"/"incorrect"/"invalid" | L'unione include anche `"neutral"` e `""` | `add_credit_if` (`marking.js:204`) usa davvero `'neutral'` quando la penalità è negativa; `set_credit`/`feedback` non passano nessuna ragione. Uno schizzo del brief con le sole tre stringhe avrebbe rifiutato il codice upstream |
| Feature "step" (`part.js:421-432, 2087-2134`, campo JSON `steps`/`stepsPenalty`) | Una parte può contenere sotto-parti "step", rivelabili con una penalità che entra in `availableMarks`/`calculateScore` | Il campo `steps` è riconosciuto e ignorato, con un `console.warn` una volta per DOMANDA (`parts/part-base.ts`, `warnStepsIgnored`: un `WeakSet` sull'oggetto `questionRef`; per una parte costruita senza domanda si ripiega su una volta per percorso, perché non c'è un contenitore su cui contare). `PartBase.steps` è sempre vuoto e i rami "steps" di `availableMarks`/`calculateScore`/`revealAnswer` non ci sono | il player v1 non ha UI di rivelazione (decisione 2 del brief del Task 8); lo schema JSON resta compatibile, così una domanda con step si carica lo stesso |
| `pre_submit` / `do_pre_submit_tasks` (`part.js:1191-1213, 1885-1946`) | `do_pre_submit_tasks` valuta la nota `pre_submit`, filtra i token `promise` del risultato e sospende la correzione finché non si risolvono | Non portati: `mark_answer` è sincrona. La nota `pre_submit` di `gapfill.jme` resta nello script incorporato e fallisce (`check_pre_submit` non esiste), quindi è registrata come nota non valida in `stateErrors` senza effetti sul risultato | le funzioni JavaScript asincrone sono rifiutate al caricamento (Task 6), quindi il filtro dei `promise` sarebbe sempre vuoto: il ramo è irraggiungibile. Non si introduce `Promise`/`async` per accomodarlo |
| `store`/`display`/`signals`/`events` (`part.js`, ~32 + ~20 + decine di siti) | Ogni metodo notifica storage SCORM, oggetto di display e pub/sub `Numbas.schedule` | Nessuno dei tre esiste: i rami guardati (`this.display && ...`, `this.store && ...`) e i `trigger` sono semplicemente assenti (`parts/part-base.ts`) | nessuna logica di correzione li legge (inventario 05 §6.6-6.7); il risultato di `submit()` è restituito direttamente invece di essere osservato |
| `setScript`/`applyScripts` (`part.js:508-548, 809-880`), campo JSON `scripts` | L'autore della domanda può sovrascrivere un metodo della parte con JavaScript arbitrario, compilato con `new Function` | Non portati: il campo `scripts` è ignorato | `new Function` è incompatibile con un motore sandboxed; la feature "custom marking JavaScript" non è nel design doc (il `customMarkingAlgorithm` JME sì, ed è portato) |
| Modalità "explore" (`part.js:1233-1246, 2148-2400`), campi JSON `nextParts`/`exploreObjective`/`suggestGoingBack` | Catena di parti sbloccate dinamicamente, con obiettivi e penalità | Non portata: i campi sono ignorati | non è fra i tipi/API del design doc |
| `MarkingResult.correct` (spec, API pubblica) | Non esiste: `Part` espone `credit`, `answered` e `markingFeedback` | Derivato come `credit >= 1` (`parts/part-base.ts`, `buildResult`) | il design doc lo chiede; `credit >= 1` è più robusto di guardare la `reason` dell'ultimo `set_credit` (inventario §10) |
| `MarkingResult.feedback` (spec, API pubblica) | `markingFeedback` è una lista di azioni `{op, credit, message, reason, format, credit_change, credit_message}` e `warnings` è una lista di stringhe separata | `feedback` è `markingFeedback` mappato su `{type, message}` seguito dai `warnings` come `type: "warning"`; il `type` è deciso dalla `reason` (`correct` → `correct`, `incorrect`/`invalid` → `incorrect`, altrimenti `info`) per QUALUNQUE `op` | il brief limitava la mappa alle voci con `op === "feedback"`, ma la voce che porta "la risposta è corretta" è un `add_credit` prodotto da `setCredit` (part.js:1994): classificarla `info` la renderebbe indistinguibile. `markingFeedback` e `warnings` restano accessibili non mappati sulla parte |
| `submit()` (`part.js:1220-1355`) | `storeAnswer(answer)` e `submit()` sono due chiamate distinte, perché la UI mette in stage la risposta prima del clic "invia" | `submit(answer?)` fa da `storeAnswer` implicito quando riceve un argomento, e ritorna il `MarkingResult` invece di lasciarlo sull'oggetto | l'API pubblica della spec è una sola chiamata sincrona; senza argomento il comportamento è identico a upstream (usa `stagedAnswer`), che è la forma di cui `submit_part` ha bisogno |
| `equal_states` nei test (`part-tests.mjs:93-98`) | L'helper esegue `assert.ok(true); return;` PRIMA del `deepEqual`: nessun test upstream confronta davvero la forma degli item di `finalised_state.states` | `equalStates` in `test/unit/parts-helpers.ts` confronta davvero, proiettando gli stati su `{op, credit, factor, reason, message, note, invalid, scale}` (tutto tranne `scope`), con l'opzione `ignoreMessages` dove il testo dipende dal dizionario i18n nostro | decisione 5 del brief del Task 8: si vuole una garanzia reale sulla forma di `FeedbackItem[]` |
| `markAlternatives` — i due blocchi protetti da `res.valid` (`part.js:1459-1462, 1537-1542`) | Scrivono `used_alternative`/`used_alternative_name` in `res.values` e in `res.script_result.states` quando `res.valid` è vero | Non portati (`parts/adaptive-marking.ts`) | `res` è un `alternative_result`, che non ha un campo `valid` (part.js:1397-1404): la condizione è sempre falsa, i due blocchi sono codice morto. `used_alternative`/`used_alternative_name` sono comunque scritti nel ramo che sceglie davvero un'alternativa (1516-1517) |
| `apply_feedback` — il ramo `action.gap` (`part.js:1830-1834`) | Riscala `credit_change` quando la voce di feedback porta un indice di gap | Non portato (`parts/mark.ts`, `annotateCreditChanges`) | nessun punto di `part.js`/`marking.js` scrive `gap` su una voce di `markingFeedback`: il ramo è irraggiungibile |
| `applyScoreLimits` (`part.js:981-983`) | `math.Fraction.fromFloat(this.settings.minimumMarks, marks)` passa due argomenti a una funzione che ne prende uno (il secondo è ignorato), e nel ramo `marks == 0` assegna il numero `0` a `creditFraction` | `Fraction.fromFloat(minimumMarks)` e `Fraction.zero` (`parts/part-base.ts`), cioè lo stesso comportamento con i tipi corretti | portare la chiamata a due argomenti non sarebbe compilabile e non cambierebbe il risultato; `creditFraction` deve restare una `Fraction` |
| `errorCarriedForwardScope` (`part.js:1645`) | `this.warnings.push("POO")` quando una parte riferita non va usata | Non portato | residuo di debug upstream |
| `makeScope` (`part.js:1062`) | `scope.part = this` | Non portato (`parts/part-base.ts`) | nessun consumatore legge `scope.part` in tutto il runtime; `part_path` (la variabile JME che gli script leggono davvero) è impostata come upstream |
| `studentAnswerAsJME` di `m_n_x` con `displayType: 'radiogroup'` (`multipleresponse.js:774-780`) | Indicizza `this.ticks[choice][answer]`, al contrario di ogni altro uso di `ticks` (che è `[answer][choice]`) | Portato identico (`parts/multiple-response-part.ts`), con un `// upstream:` al sito | è un baco upstream che produce risultati diversi solo per griglie non quadrate; replicarlo tiene il marking adattivo fedele all'originale |
| `storeAnswer` delle scelte multiple | Accetta solo la matrice `ticks` (`boolean[][]` indicizzata `[risposta][scelta]`), costruita dalla UI con `storeTick` | Accetta anche la forma pubblica di `Answer`: un `number` (indice della scelta) per `1_n_2`, un `boolean[]` per `m_n_2`, e per `m_n_x` **entrambe** le orientazioni della griglia — quella "naturale" `[scelta][risposta]` e la matrice `ticks` `[risposta][scelta]`; le forme sono distinte dalle dimensioni del valore (`parts/multiple-response-part.ts`) | l'API pubblica non ha una UI che costruisca la matrice; la matrice `ticks` resta accettata invariata perché i fixture `unitTests` upstream la usano (risoluzione 1 del Task 8). Per `m_n_x` con griglia QUADRATA le due orientazioni non sono distinguibili e si assume `ticks`, come upstream — è anche la forma che `Question#toState` salva, qualunque sia quella inviata (v. README, sezione "Inviare una risposta") |
| `jme/display.subvars` | `Notation.prototype.subvars` compila con `this.compile`, cioè con il parser della notazione scelta | `subvars(expr, scope, parser?)` accetta un parser opzionale (`packages/engine/src/jme/display.ts`); senza, usa quello standard | il tipo di parte `jme` compila `mustmatchpattern` con il parser dei pattern (parts/jme.js:344-345). È l'unico frammento di `jme-notations.js` che serve ai tipi in ambito |
| `hasStagedAnswer` e `null` (`part.js:1368-1370`, `gapfill.js:167-171`) | `return !(this.stagedAnswer == undefined)` — confronto LASCO, quindi `null` conta come "nessuna risposta"; `GapFillPart#storeAnswer` inoltra `answer[i]` invariato, cioè `undefined` per un indice mancante | Identico: `hasStagedAnswer()` è falso sia per `undefined` sia per `null` (`parts/part-base.ts`), e `GapFillPart#storeAnswer` inoltra `answers[i]` senza sostituirlo (`parts/gapfill-part.ts`). `PartBase#storeAnswer` accetta quindi `Answer \| undefined`, un tipo più largo di quello del brief | `Answer` ammette `null` (una parte `information` non ha risposta) e l'API pubblica lo espone: trattarlo come una risposta vera fa proseguire la correzione su un valore che i `setStudentAnswer` dei tipi non sanno gestire (`MultipleResponsePart` chiamerebbe `.map` su `null`). Sostituire `undefined` con `null` in un gap senza risposta dà invece una risposta LETTERALE: per un `numberentry` la stringa `"null"`, perché `cleanAnswer` fa `String(answer).trim()` |
| `errorCarriedForwardScope` — parte riferita inesistente (`part.js:1637`) | `this.question.getPart(vr.part)` ritorna `undefined` e la riga dopo chiama `p2.shouldUseInAdaptiveMarking()`: `TypeError` con un messaggio illeggibile | Errore esplicito `part.marking.variable replacement part not found` (chiave nostra, `parts/part-base.ts`) | il messaggio finisce nel feedback dello studente attraverso `markAdaptive`/`markAgainstScope`: un `TypeError` di JavaScript lì non è accettabile. La chiave è nuova perché upstream non ne ha una |
| Campo JSON `prompt` (`part.js`) | `part.js` non legge MAI `prompt`: lo usano solo il tema (che lo passa per `jme.contentsubvars`) ed `exam-to-xml.js:658` | `loadFromJSON` copia `prompt` in `PartBase.promptHtml`, GREZZO (`parts/part-base.ts`) | l'API pubblica della spec prevede il testo della consegna sulla parte. La sostituzione `\var{}`/`{...}` resta al Task 9, che ha lo scope della domanda con le variabili già generate |
| `setDirty` (`part.js:1021-1030`) | La propagazione di `isDirty` alla parte madre è DENTRO `if(this.display)`: senza oggetto di display il flag del genitore non viene mai aggiornato | Propagazione incondizionata (`parts/part-base.ts`) | non c'è un display da cui dipendere, e `isDirty` è un flag dell'API pubblica: legarlo alla presenza di una UI lo renderebbe inutilizzabile. Cambia un valore osservabile — un gapfill il cui gap è stato modificato risulta `isDirty` anche senza display — quindi è annotato qui e con un `// upstream:` al sito |
| Errori di griglia in `m_n_x` (`multipleresponse.js:501-511`) | Un `layoutExpression` che non si valuta, o un `layout.type` sconosciuto, producono un `TypeError` (`jme.unwrapValue(undefined)`, `layoutTypes[nome]` indefinito) | Errore esplicito `part.mcq.invalid layout` con il tipo di griglia come parametro (chiave nostra, `parts/multiple-response-matrix.ts`) | riusare `part.mcq.matrix not a list` (il primo tentativo di questo port) sviava l'autore della domanda verso la matrice dei punteggi, che non c'entra |
| `Question`, caricamento asincrono (`question.js:772-918`) | `finaliseLoad` cabla un grafo di eventi su `Numbas.schedule.SignalBox` (Promise): l'ordine dei rami "paralleli" (costanti, funzioni, ruleset) dipende dall'ordine di risoluzione dei microtask, e il chiamante deve fare `q.generateVariables()` e attendere il signal `ready` | Sequenza sincrona nello stesso ordine di dipendenza, con l'ordine del sorgente per i rami paralleli: preambolo → costanti → funzioni → ruleset → `variablesTodo` → generazione → scope finale → parti → punteggio (`question/question.ts`). `loadQuestion(json, opts): Question` ritorna una domanda già pronta, non una `Promise` | `schedule.js` serve solo a orchestrare il caricamento asincrono e la barra di progresso (inventario 06 §7); l'unica cosa che upstream ha davvero bisogno di attendere sono le funzioni JavaScript asincrone, che qui sono rifiutate al caricamento. Un ordine esplicito è anche riproducibile: quello upstream non lo è |
| `partsMode: "explore"` (`question.js:426-458`, 673-687, 1215-1235, 1303-1315, 1363-1401) | Obiettivi, penalità, parti generate al volo, `maxMarks`, `showAllParts` | `loadQuestion` lancia `question.parts mode not supported` (chiave nostra); `objectives`, `penalties`, `maxMarks`, `objectiveVisibility`, `penaltyVisibility` e `showAllParts` sono letti e ignorati (`question/load.ts`) | decisione 1 del brief del Task 9: la modalità "explore" è una feature strutturale della domanda che SAVINT non usa. Fallire al caricamento è meglio che caricare a metà una domanda che si comporterebbe in modo diverso |
| `preamble.js` (`question.js:1183-1201`) | Eseguito con `new Function(['question'], js)`, con accesso completo all'oggetto domanda, allo scope e al globale `Numbas` | `loadQuestion` lancia `question.preamble not supported` (chiave nostra); con `LoadOptions.ignorePreamble` lo ignora con un `console.warn`. In tutto `src/question/` non c'è nessun `new Function` né `eval` | decisione 2 del brief: eseguire JavaScript arbitrario dell'autore della domanda **lato server** (il motore ricalcola i punteggi) è un rischio che la spec non discute. `preamble.css` è ignorato in silenzio: il motore non produce HTML |
| `extensions` (`question.js:508-513`, 647-671) | `addExtensionScopes` aggiunge lo scope di ogni estensione registrata in `Numbas.extensions`, e lancia `question.required extension not available` per quelle mancanti | Nessun meccanismo di estensioni: una lista `extensions` non vuota lancia subito `question.required extension not available` (`question/load.ts`) | decisione 3: fail-fast. Una domanda che usa una funzione fornita da un'estensione fallirebbe comunque, ma più tardi e con un messaggio che parla di una funzione JME sconosciuta invece che dell'estensione mancante |
| Funzioni personalizzate asincrone (`jme-variables.js:87-127`, 308-312) | `language: "javascript"` con `type: "promise"` ritorna un `TPromise` che `makeVariablesPromise` attende: è il motivo per cui tutta la generazione delle variabili è `async` | `loadQuestion` lancia `question.function.async not supported` (chiave nostra) prima ancora di costruire le funzioni (`question/load.ts`); `makeFunction` ha comunque la sua rete di sicurezza (`jme.variables.async function not supported`, Task 6) | decisione 4: l'API è sincrona (`loadQuestion(...): Question`). Rifiutare esplicitamente è meglio che bloccarsi o produrre risultati diversi dall'oracolo |
| Casualità per domanda (`question.js:844-867`) | Non esiste un seme di domanda: ogni giro del ciclo `variablesTest` consuma `Math.random` globale. Ricreare la stessa variante è impossibile, e per questo lo stato salvato deve contenere i valori delle variabili non deterministiche (`storage.js:445`) | `LoadOptions.seed` semina un `Rng` che vive sullo `Scope` della domanda (`question/load.ts`); `Question#regenerate(seed)` ricostruisce la domanda con un altro seme. `QuestionState` **non** salva le variabili: si rigenerano dal seme (`question/state.ts`) | decisione 5 del design doc: il punteggio va ricalcolabile lato server. È l'unica divergenza che cambia il *formato* dello stato salvato rispetto a `question_suspend_data` |
| `Question#resume` (`question.js:935-1070`) | Ripristina da `Numbas.storage`/SCORM: valori delle variabili, `interactive_state` dei token, `pre_submit_cache`, feedback già calcolato, parti extra della modalità explore | `restoreQuestion(json, state, opts?)` = `loadQuestion` con il seme dello stato + `storeAnswer`/`submit` delle parti già risposte (nell'ordine "sorgenti prima di chi le usa", question.js:1004-1033) + `revealAnswer`/`getAdvice` (`question/state.ts`) | non c'è storage SCORM da cui leggere: lo stato è un oggetto JSON dell'applicazione. Il feedback non è salvato ma **ricalcolato**, il che è possibile solo perché le variabili sono deterministiche |
| `Question#getPart` (`question.js:1202-1213`) | Lancia `question.no such part` se il percorso non esiste | Ritorna `undefined` (`question/question.ts`) | è il contratto che il Task 8 ha già fissato: i rami di errore della correzione adattiva controllano `undefined` e producono un messaggio di feedback invece di un'eccezione. La chiave `question.no such part` resta nei dizionari per chi voglia segnalarlo |
| `setErrorCarriedForwardBackReferences` (`question.js:689-698`) | `q.getPart(r.part)` lancia se la parte riferita non esiste: la domanda non si carica affatto | Il riferimento a una parte inesistente è ignorato al caricamento (`question/parts.ts`); l'errore arriva alla correzione, come `part.marking.variable replacement part not found` | conseguenza della riga precedente. Il caso è coperto dal test 'Adaptive marking error when referenced part doesn't exist' (part-tests.mjs:1098-1130), dove upstream carica la domanda e fallisce solo al `submit` — quindi il comportamento osservabile coincide con quello del test |
| `Question#error` (`question.js:249-260`) | Vorrebbe accumulare la catena delle chiavi (`[message].concat(originalError.originalMessages \|\| [])`) ma riassegna `originalError` a un `Error` nuovo la riga prima: la catena si ferma sempre a due chiavi e la causa vera è persa | La catena è conservata davvero: `new JmeError("question.error", ..., new JmeError(message, args, originalError))`, e `engineErrorKeys(e)` la percorre (`question/question.ts`) | si porta l'intento del codice, non il baco: senza la causa vera un errore di compilazione dentro la definizione di una variabile non sarebbe più diagnosticabile |
| `prompt` di una parte | Il tema passa `prompt` per `jme.contentsubvars` ogni volta che costruisce l'HTML | `PartBase.promptHtml` è sostituito **una volta sola** al caricamento della domanda, con `substituteHtml` sullo scope della parte (`question/parts.ts`) | il motore non ha un display che ricostruisca l'HTML: la sostituzione va fatta quando le variabili ci sono. Vale anche per `statement` e `advice` (`Question#statementHtml`/`adviceHtml`) |
| `Part#revealAnswer`/`lock` (`part.js:2238-2261`, `gapfill.js:147-151`) | `revealAnswer` è composta per tipo con `util.extend`: la versione di `gapfill` rivela i gap e poi quella base rivela sé stessa | Una sola implementazione su `PartBase`, che ricorre sui `gaps` (vuoti per gli altri tipi) — `parts/part-base.ts`. Nessun `display`, nessun evento, nessun ramo `steps` | il risultato osservabile è identico e non serve un override per `gapfill`. I due metodi mancavano al Task 8 perché hanno senso solo dentro una domanda |
| Nota `pre_submit` di uno script di correzione (`part.js:1904-1945`, `marking.js:348-366`) | `mark_answer` valuta `pre_submit` a parte (`evaluate_note`) per far partire i compiti asincroni, e poi `markingScript.evaluate` la ricalcola insieme a tutte le altre note | `MarkingScript#evaluate` **salta** la nota `pre_submit` (`marking/marking-script.ts`, via il nuovo parametro `targets` di `Script.evaluate`) | il port è solo sincrono (risoluzione 2 del Task 8) e `check_pre_submit` non è portato: la nota fallirebbe **dopo** aver rieseguito i `submit_part` sui gap di un `gapfill`, lasciandone uno con `shouldResubmit` acceso — upstream lo ripulisce nell'iterazione successiva, che qui non arriva perché l'errore interrompe la `map`. Saltarla toglie sia la divergenza (test 'Adaptive marking order', part-tests.mjs:1035-1041) sia il doppio invio dei gap |
| `compute_note`, nome che non è una nota (`marking.js:545`) | `todo[name].vars` senza controlli: con un nome che non è una nota va in `TypeError`, che esce da `compute_note` e che `computeVariable` riavvolge in `jme.variables.error computing dependency` — cioè la nota che lo riferisce fallisce | `computeNote` rilancia l'errore vero (`jme.variables.variable not defined`) invece del `TypeError` (`marking/compute-note.ts`) | `ignore_note_errors` è pensato per gli errori *dentro* una nota definita, non per un nome inesistente. Verificato sull'oracolo (`packages/engine/oracle`, commit 0f0ea33) sul caso 'Error in mark note' (part-tests.mjs:1250-1261): upstream registra `state_errors` per `a`, `q`, `z` **e** `mark`, e `marking_result.answered` è falso. Il primo tentativo del port, che ignorava l'errore, rendeva `mark` valida e la parte "risposta". Cambia solo il testo del messaggio (la chiave vera invece di "Error computing referenced variable") |
| `Script.evaluate` (`jme-variables.js:911-918`) | Passa sempre tutte le note a `makeVariables` | Terzo parametro opzionale `targets`, inoltrato a `makeVariables` (che lo prevede già, jme-variables.js:343) — `variables/note-script.ts` | serve a `MarkingScript` per saltare `pre_submit` (riga sopra). Senza il parametro, il comportamento è identico a upstream |
| `local_definitions.functions` (`question.js:877`) | `Object.keys(q.functionsTodo)` su un ARRAY (question.js:566): produce gli indici `"0"`, `"1"`, … invece dei nomi delle funzioni. Chi legge quel campo ne risente: `scope.unset(question.local_definitions)` (parts/jme.js:333-335) cancella per nome e quindi **non cancella nessuna funzione**, e il valore `question_definitions` passato agli algoritmi di correzione personalizzati (part.js:1860) elenca indici che non nominano niente | I nomi veri delle funzioni della domanda (`question/question.ts`) | è un baco senza alcun beneficio osservabile da riprodurre. Con la correzione, le funzioni definite dalla domanda **sono** davvero rimosse dallo scope in cui si valuta l'espressione dello studente in una parte `jme` — che è quello che l'autore della domanda si aspetta, ed è la stessa regola che upstream applica correttamente a variabili e ruleset (che sono dizionari, quindi lì `Object.keys` dà i nomi giusti). Cambia due valori osservabili: lo scope di `JMEPart#getCorrectAnswer` e `question_definitions["functions"]` |
| Risultato del marking adattivo fallito (`part.js:1136-1152`, 1168-1183) | Il literal costruito nei due rami d'errore non ha la chiave `answered`: `this.answered = result.answered` (part.js:1275) mette `undefined` su una proprietà che il prototipo dichiara booleana (`part.js:679`) | `answered: false` esplicito (`parts/adaptive-marking.ts`, `emptyErrorResult`) | `MarkingResults` è un tipo, non un oggetto libero: `answered` è `boolean`. Il valore falso ha lo stesso effetto su ogni consumatore upstream (`if(this.answered)`, `q.validate()`), ma è serializzabile in `PartState` senza perdere la chiave. Il differenziale (`known-divergences.json`, `upstream/09`) lo registra |
| Forme plurali dei messaggi (`localisation.js`, cataloghi `locales/*.json`) | i18next: il messaggio annida un'altra chiave (`$t(mark)`, `$t(was)`) e la forma la sceglie la regola di plurale della lingua a partire da `count` — le tre chiavi in ambito del motore (`feedback.you were awarded`, `feedback.taken away`, `part.marking.maximum scaled down`) passano tutte di lì | Nessun annidamento e nessuna libreria: il messaggio è scritto per esteso e la forma plurale sta in `<chiave>_plural`, scelta da `t()` quando `count != 1` (`i18n/index.ts`) | il motore non porta i18next (riga «Messaggi» in cima a questa tabella); la regola "singolare solo per esattamente uno" è quella che i18next applica a inglese e italiano, le uniche due lingue del motore. Senza questo, `t()` rendeva "1 marks"/"1 punti": lo ha trovato il differenziale (Task 10) |
| `texsplit`, riconoscimento di `\var`/`\simplify` (`jme.js:443-494`) | `cmdre = /^((?:.\|[\n\r])*?)\\(var\|simplify)/m` riconosce `\var`/`\simplify` come prefisso LETTERALE: `\varphi`, `\varepsilon`, `\vartheta`, `\varsigma`, `\varrho`, `\varpi` (notazione trigonometrica ordinaria) cominciano con quelle quattro lettere e sono scambiati per un `\var` a cui manca l'argomento, che lancia `jme.texsubvars.missing parameter` — verificato che upstream fa lo stesso (`packages/engine/oracle`, commit 0f0ea33: `Numbas.jme.texsplit("\varphi")` lancia lì il messaggio identico) | `cmdre` ha in più il lookahead negativo `(?![a-zA-Z])`: `\var`/`\simplify` sono riconosciuti solo quando il nome del comando non prosegue in altre lettere (`packages/engine/src/jme/subvars.ts`). Uno spazio (o qualunque carattere non alfabetico) fra il comando e la sua graffa resta un errore, come upstream — verificato anche quello sul runtime upstream: `texsplit("\var {a}")` lancia `jme.texsubvars.missing parameter` sia lì che qui | `\varphi` e le altre lettere greche "var*" sono notazione LaTeX ordinaria, non un `\var{}` senza argomento: senza la correzione, `substituteHtml`/`loadQuestion` lanciano su qualunque esercizio (es. trigonometria) che le usi nell'enunciato o in una consegna, e la domanda non si può scrivere, anteprimare né risolvere. Riprodotto da `jme-subvars-tex.test.ts` |
| Arità di `texOps` (`jme-display.js`, es. `sqrt`/`int`/`mod`/`defint`/...) | Alcune voci di `texOps` indicizzano `texArgs` per posizione (es. `texArgs[0]`) senza controllare che ci sia davvero un argomento in quella posizione: una funzione chiamata con un'arità sbagliata (es. `sqrt()`, zero argomenti) produce `undefined` concatenato senza errore nella stringa resa (`"\sqrt{ undefined }"`). ALTRE voci (`abs`, `fact`, `decimal`, `transpose`, `ln`, `root`, `if` — quando manca il PRIMO argomento che leggono) dereferenziano `.tok` su un elemento di `tree.args` assente e lanciano invece un `TypeError` grezzo, non tradotto, mai capitato dentro un `Numbas.Error`: stesso meccanismo (un'indicizzazione posizionale non controllata), sintomo diverso. Verificato che upstream ha entrambe le classi, identiche (`packages/engine/oracle`, commit 0f0ea33): `exprToLaTeX("sqrt()", ...)` rende `"\sqrt{ undefined }"` anche lì, `exprToLaTeX("abs()", ...)` lancia lì lo stesso `TypeError`. A differenza degli operatori, la cui arità la sintassi shunting-yard fissa a tempo di analisi (`12*x^` lancia già `jme.shunt.not enough arguments`, sia upstream che nel port), la chiamata di funzione `nome(...)` accetta sintatticamente qualunque numero di argomenti | `texFunction` verifica, prima di chiamare la voce di `texOps`, l'arità contro una TABELLA ESPLICITA per nome (`TEX_FUNCTION_MIN_ARITY`, `packages/engine/src/jme/display-texifier.ts`), scritta leggendo ogni renderer e verificata chiamandolo direttamente con `texArgs` di lunghezza crescente — non dedotta da una funzione REGISTRATA nello scope con lo stesso nome: un primo tentativo lo faceva (via `enumerate_signatures`/`scope.getFunction`), ed era sbagliato, non solo impreciso — `int` registra un cast a intero (`[TNum] → TInt`, un argomento, `builtins/type-casting.ts`) senza alcuna relazione con la notazione dell'integrale indefinito `int(espressione, variabile)`, e `diff` registra solo la firma a due argomenti (`[TExpression, TString]`, `builtins/differentiation.ts`) perché il terzo — il grado di derivazione — è una convenzione di sola visualizzazione senza valutatore: quel tentativo rifiutava `diff(y,x,2)`/`int(x,y)`, validi, e lasciava passare `int(x)`/`defint(...)` incompleti, perché `defint` non è nemmeno una funzione registrata. Le voci con un argomento facoltativo/di sola visualizzazione (il grado di `diff`/`partialdiff`, la base di `log`) hanno nella tabella il minimo vero, non quello "tipico"; `dot`/`cross` non ci sono, la loro arità valida è "esattamente 1 o 2", gestita a parte. Un nome assente dalla tabella non ha un'arità nota da controllare e passa, come upstream. Se l'arità non basta lancia `jme.display.wrong number of arguments`. `texOp` (gli operatori) non è toccato: la sua arità è già garantita dal parser | un'espressione attesa incompleta (una risposta d'esempio che un docente non ha finito di scrivere) renderebbe una formula plausibile contenente la parola "undefined" — o, per l'altra classe, farebbe crashare la correzione con un messaggio tecnico incomprensibile invece di un errore del motore riconoscibile. Due punti dell'applicazione (`src/components/esercizi/player/parti/espressione.tsx`, `src/lib/esercizi/editor/verifica.ts`) aggirano la prima classe cercando la stringa "undefined" nel LaTeX reso: restano, come difesa in profondità, ma non sono più l'unica barriera per le voci della tabella. Riprodotto da `jme-display.test.ts`; le voci con argomenti facoltativi (`diff(y,x,2)`, `int(x,y)`, `defint(x^2,x,0,1)`, `log(x)`) sono anche nel corpus differenziale (`display.diff.test.ts`), rese byte per byte identiche all'oracolo |
